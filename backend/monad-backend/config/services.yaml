# This file is the entry point to configure your own services.
# Files in the packages/ subdirectory configure your dependencies.

# Put parameters here that don't need to change on each machine where the app is deployed
# https://symfony.com/doc/current/best_practices.html#use-parameters-for-application-configuration
parameters:
    # Hetzner Object Storage requires path-style addressing; AWS does not care.
    env(S3_USE_PATH_STYLE_DEFAULT): '1'
    env(S3_ENDPOINT): ''

services:
    # default configuration for services in *this* file
    _defaults:
        autowire: true      # Automatically injects dependencies in your services.
        autoconfigure: true # Automatically registers your services as commands, event subscribers, etc.

    # makes classes in src/ available to be used as services
    # this creates a service per class whose id is the fully-qualified class name
    App\:
        resource: '../src/'
        exclude:
            - '../src/DependencyInjection/'
            - '../src/Entity/'
            - '../src/Kernel.php'
            - '../src/OpenApi/ErrorResponseSchemas.php'

    # add more service definitions when explicit configuration is needed
    # please note that last definitions always *replace* previous ones

    # S3 Service configuration
    # Object storage is Hetzner, not AWS. The env names keep their AWS_* prefix because they are
    # the SDK's vocabulary, but S3_ENDPOINT is what actually points them at the project's bucket —
    # the same tenancy the csid fleet captures and simulation artefacts live in.
    App\Service\S3Service:
        arguments:
            $awsRegion: '%env(AWS_S3_REGION)%'
            $awsBucket: '%env(AWS_S3_BUCKET)%'
            $awsAccessKeyId: '%env(AWS_S3_ACCESS_KEY_ID)%'
            $awsSecretAccessKey: '%env(AWS_S3_SECRET_ACCESS_KEY)%'
            $presignedUrlExpiry: '%env(AWS_S3_PRESIGNED_URL_EXPIRY)%'
            $endpoint: '%env(default::S3_ENDPOINT)%'
            $usePathStyle: '%env(bool:default:S3_USE_PATH_STYLE_DEFAULT:S3_USE_PATH_STYLE)%'

    # Operator-authored lab bundle. A file, not a table: it describes physical reality and is
    # edited by whoever is standing next to the hardware.
    App\Service\LabConfigService:
        arguments:
            $configPath: '%kernel.project_dir%/config/lab/lab.json'

    # OpenAPI Decorators - chained in order
    # AuthDecorator wraps the base OpenApiFactory
    App\OpenApi\AuthDecorator:
        decorates: 'api_platform.openapi.factory'
        arguments: ['@App\OpenApi\AuthDecorator.inner']

    # QuestDecorator wraps AuthDecorator
    App\OpenApi\QuestDecorator:
        decorates: 'App\OpenApi\AuthDecorator'
        arguments: ['@App\OpenApi\QuestDecorator.inner']

    # StorageDecorator wraps QuestDecorator
    App\OpenApi\StorageDecorator:
        decorates: 'App\OpenApi\QuestDecorator'
        arguments: ['@App\OpenApi\StorageDecorator.inner']
